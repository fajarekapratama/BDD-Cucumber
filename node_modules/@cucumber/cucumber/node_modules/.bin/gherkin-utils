#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../gherkin-utils/bin/gherkin-utils" "$@"
  ret=$?
else 
  node  "$basedir/../../../gherkin-utils/bin/gherkin-utils" "$@"
  ret=$?
fi
exit $ret
